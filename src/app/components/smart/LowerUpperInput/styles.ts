import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  input: {
    width: "174px",
    height: "50px"
  },
  icon: {
    paddingLeft: "14px",
    paddingRight: "14px"
  }
}));
