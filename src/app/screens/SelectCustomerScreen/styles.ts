import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  root: {
    width: "100%",
    paddingLeft: "130px",
    paddingRight: "130px"
  },
  container: {
    paddingTop: "24px"
  },
  rightItemGrid: {
    textAlign: "right"
  }
}));
